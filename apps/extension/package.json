{
  "name": "@demo/extension",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "dev": "tsc --watch",
    "copy-assets": "cp manifest.json popup.html dist/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@demo/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "typescript": "^5.4.5"
  }
}
