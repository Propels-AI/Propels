{
  "name": "@demo/root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter webapp dev",
    "build": "pnpm --filter webapp build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter webapp preview"
  }
}
